# API Configuration
# Base URL for the FastAPI backend (todo AI chatbot API)
# Used for all API calls from the frontend to backend services
NEXT_PUBLIC_API_URL=http://localhost:8000

# Better Auth Configuration
# Base URL for Better Auth authentication service
# Should match the frontend URL in production (where auth pages are served)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
